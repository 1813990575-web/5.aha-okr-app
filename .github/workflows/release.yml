name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build application
        run: npm run electron:build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Get version from package.json
        id: get_version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "TAG_NAME=v$VERSION" >> $GITHUB_OUTPUT
      
      - name: Check build outputs
        run: |
          ls -la ./release/
      
      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ./release/AhaOKR-*-arm64.dmg
            ./release/AhaOKR-*-x64.dmg
            ./release/AhaOKR-*-x64.zip
          name: AhaOKR ${{ github.ref_name }}
          body: |
            ## AhaOKR ${{ github.ref_name }}

            ### 安装说明
            1. 根据你的 Mac 芯片选择对应版本：
               - Apple Silicon (M1/M2/M3): 下载 `AhaOKR-*-arm64.dmg`
               - Intel Mac: 下载 `AhaOKR-*-x64.dmg` 或 `AhaOKR-*-x64.zip`
            2. 打开 dmg 文件，将应用拖到 Applications 文件夹
            3. 首次打开可能需要到 系统设置 > 隐私与安全性 中允许运行

            ### 系统要求
            - macOS 10.12 或更高版本
          draft: false
          prerelease: false
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
